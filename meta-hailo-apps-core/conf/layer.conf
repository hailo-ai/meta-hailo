# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-hailo-apps-core"
BBFILE_PATTERN_meta-hailo-apps-core = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-hailo-apps-core = "8"

LICENSE_PATH = "${LAYERDIR}/licenses/"

LAYERDEPENDS_meta-hailo-apps-core = "core meta-hailo-libhailort"
LAYERSERIES_COMPAT_meta-hailo-apps-core = "scarthgap"

CORE_IMAGE_EXTRA_INSTALL:append = " packagegroup-hailo-apps-core${@bb.utils.contains('HAILO_TARGET', 'core-image-hailo', '', '-dev-pkg', d)}"
